[project]
name = "copilot-agent"
version = "0.1.0"
description = "M365 Copilot automation agent"
requires-python = ">=3.11"
dependencies = [
    "websockets>=13",
    "rich>=13",
    "prompt-toolkit>=3",
]

[dependency-groups]
dev = []
